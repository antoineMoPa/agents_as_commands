# find_large_files

Lists text files in the current git repository with more than 800 lines. Gitignored files,
lock files, common data/media/archive formats, and binary files are excluded.

```bash
find_large_files
find_large_files -n 1200
```
